# 田んぼNDVIマップ — 事前計算＋静的配信

県内の田んぼ（筆ポリゴン）ごとの NDVI を **週1回まとめて計算** し、結果を **静的ファイル** として公開する仕組みです。
利用者（農家・普及員）はブラウザで地図を開き、田んぼをタップするだけ。利用時に計算は発生しないため、
サーバー費用はほぼゼロで、利用者が増えても遅くなりません。

```
 筆ポリゴン(農水省)  Sentinel-2(Copernicus)
        │                  │
        ▼                  ▼
  build_cells.py     update_ndvi.py  ← Google Earth Engine で区画平均を計算
  (区画をグリッドに分割)  (未計算の観測日だけ追記)
                     trend.py        ← 過去の年の作物の見分けと田の中の差（生育傾向マップ）
        │                  │
        └──────┬───────────┘
               ▼
       site/data/           ← GitHub Actions が週1回更新し、GitHub Pages で配信
        ├ index.json          （セル一覧・更新日）
        └ cells/<id>/
           ├ parcels.geojson  （区画の形（約1m単位に丸め）・有効画素の期待数）
           ├ inner.geojson    （計算用: 畦畔を避けて5m内側に縮めた形）
           ├ px_<年>.png/json （圃場内マップ: 晴れた日の10m画素のNDVI）
           ├ trend.png/json   （生育傾向マップ: 年ごとの作物の手がかりと、6月ごろ・8月ごろの田の中の差）
           ├ hist.json        （生育傾向のグラフ用: ndvi.json より前の年の 5/10〜9/30 の区画平均。形式は ndvi.json と同じ）
           └ ndvi.json        （観測日 × 区画の NDVI と有効画素率。50%未満の観測は保存しない）
               │
               ▼
       site/index.html      ← 表示範囲のセルだけ読み込む地図ビューア
```

## できること（ビューア）
- 航空写真の上に区画。選んだ日付の NDVI で色分け（地域全体の生育の見え方）
- 区画をタップで最大20枚を比較。直近12か月／前年との重ね／移植後日数
- 近隣の田の平均（半径可変）との比較、要約表
- 雲・かすみで一斉に落ちた観測日を自動除外（詳細設定で戻せる）
- 「この場所のリンクをコピー」で、位置と選択した田んぼをそのまま共有
- 塗り方「生育傾向マップ」: 田を選ぶと、その中を「生育弱傾向 ←→ 生育良傾向」で色分け（追肥を検討する所の参考）。2022年からの **稲の年だけ** を重ねる。
  スコア: 年ごとに6月ごろ（E）・8月ごろ（L）の「その田の平均との差」をとり、後半に伸びた所（L≧E）は L、伸びなかった所は (E+L)/2 を平均。
  ずっと大人しい所・後半に落ちる所は低く、出だしが遅くても毎回後半に追いつく所（田面が深いなど）は低くならない。
  2年3作で麦・大豆を作った年は自動で見分けて除く（稲と麦・大豆では、湿った所の強い・弱いが逆になるため）
  グラフは、稲の年の 5/10〜9/30 の推移を1日ごとに線でつないで平均したもの（実線）と
- 生育傾向マップで田を選ぶと、圃場の評価（S・A・B・C・D）と特徴を2〜3行で出す。100点満点:
  広さ30（500m²で0点〜1haで満点、対数）・生育のそろい20（田の中のばらつき＝上下1割を除いた幅 2〜15%）・
  形15（下記）・深い所の少なさ10（前半に出遅れて後半に追いつく所の割合 0〜40%）・
  例年の旺盛さ15（8月ごろの NDVI の稲の年の平均の、同じセルの中での順位）・年ごとの安定10（年ごとの差 2〜8%）。区切り S≧81・A≧70・B≧45・C≧27
  （県全体の稲の田 約12万枚で、獣害の減点のあとの点が 6%・20%・50%・20%・5% になるように）
  形（cells/<id>/shape.json, pipeline/shape.py）: 機械作業1回の時間 T = A/(W·v) + N·t_t + Σ t_b·n(α) + Σ W²/(4·tan(α/2))/c_m
  （W=2.4m, v=1m/s, t_t=15s, t_b=20s, c_m=1m²/分。N は最も効率のよい向きで幅 W ごとに田を切ったときの行程の数、
  n(α) は隅の切り返しの回数）を、同じ広さの 1:3 の長方形と比べたロスと、周長（畦畔）の倍率から、
  形の点 = 15 ×（1 −（0.7×ロス/20% + 0.3×畦畔の増え/40%））
  獣害の起きやすさ（cells/<id>/wild.json, pipeline/wild.py）: ESA WorldCover 2021 の森と筆ポリゴンから、田の縁から 0〜30m・30〜100m・100〜200m の森の割合、
  200m以内に森がある方位（16方位）、周り300mの田の割合を田ごとに作る。危なさ R = 0.6×近さ（0.6×f30+0.4×f100、40%で満点）
  + 0.25×囲まれ（森の方位 4〜12）+ 0.15×孤立（周りの田 40%→0〜5%→1）で、R×20 点を引く
  （県全体で、半分の田が2点以下・4枚に1枚が11点以上。平野の真ん中はほぼ0、谷あい・山すその田が大きい）
  区切り S≧80・A≧68・B≧46・C≧28（県全体の稲の田 約12万枚で 5%・20%・50%・20%・5% になるように。山際の減点の前）

## セットアップ（1回だけ）

専門用語なしの手順書は `SETUP_やさしい版.md` を参照。

### 1. Google Earth Engine（計算エンジン）
1. Google Cloud プロジェクトを Earth Engine に登録（非営利・研究用途。試験場の研究として登録）
2. Cloud コンソール → IAM → **サービスアカウント** を作成し、JSON 鍵をダウンロード
3. そのサービスアカウントに `Earth Engine Resource Viewer` と `Service Usage Consumer` のロールを付与
4. Cloud コンソールで `Earth Engine API` を有効化（まだなら）

### 2. GitHub リポジトリ
1. このフォルダをそのままリポジトリにする（**Public** にする。無料プランでは Public リポジトリだけが Pages で公開でき、Actions の実行時間も無制限）
2. Settings → Secrets and variables → Actions に3つ登録
   - `EE_SERVICE_ACCOUNT` … サービスアカウントのメールアドレス
   - `EE_PRIVATE_KEY` … JSON 鍵ファイルの中身をそのまま貼る
   - `EE_PROJECT` … Cloud プロジェクトID
3. Settings → Pages → Source を `gh-pages` ブランチにする（初回の Actions 実行後にブランチができます）

### 3. 区画データ
`config.yaml` の `parcel_sources` に筆ポリゴンの入手先を書きます。
- 農林水産省「筆ポリゴンデータ」ページの **福井県 FlatGeobuf（分割ファイル）** の URL を並べる、または
- 筆ポリゴン公開サイト（open.fude.maff.go.jp）から市町ごとに GeoJSON を落として `parcels/` に置く
`bbox` で範囲を絞れます。まずは1つの市町か地区で試すのが安全です。

### 4. 初回実行
Actions → `weekly-ndvi` → **Run workflow**。初回は過去24か月分をさかのぼるので数時間かかります。
1回で終わらなくても、途中まで保存して次回に続きから計算します（`max_minutes` を超えると自動で切り上げ）。
終わったら `https://<ユーザー名>.github.io/<リポジトリ名>/` で開けます。

## 運用
- 5〜9月は毎日、それ以外は毎週月曜の 4:23（JST）に自動更新。手動実行も可
- NDVI を公開したあと、残り時間で生育傾向マップのデータを作ってもう一度公開する。最初は全セルで数日かかる
  （`trend_center` に近いセルから。できた年は作り直さない。毎年9月に入ると、その年の分を足す）
- 筆ポリゴンが年次更新されたら、`config.yaml` を直して **Run workflow の `rebuild_cells` にチェック**
- 1セル ≒ 2.2km×1.85km。県全体で数百セル。週次更新は数分〜数十分
- ログは Actions の実行画面で見られます。1セルの失敗は飛ばして続行します

## 設定の要点（config.yaml）
| 項目 | 意味 |
|---|---|
| `only_paddy` | 田（land_type=100）だけを対象にする |
| `edge_m` | 畦畔を避けて内側に縮める距離。10m画素なら5mが目安 |
| `history_months` | 初回にさかのぼる月数（前年重ね表示には24） |
| `max_scene_cloud` | シーン雲量がこれ以上の画像は使わない。空欄なら画像全体の雲量では捨てない（区画ごとに判定） |
| `cloud_score_min` | 雲判定 Cloud Score+ の晴れ度（0〜1）。これ以上の画素だけ使う。変えると次回の更新で全期間を計算し直す |
| `landsat` | Landsat 8/9（NASA HLS, 30m）も使う。Sentinel-2 がない日の観測を補う（グラフでは白抜きの点） |
| `landsat_min_pixels` | Landsat の30m画素がこれ未満しか入らない小さい区画には Landsat を使わない |
| `radar` | Sentinel-1 レーダー（雲を通す）も使う。指標は VH の後方散乱（dB）。地図では範囲ごとに晴れた日の NDVI との関係から推定 NDVI に換算し、5/10〜7/31 の空白を埋める（グラフではひし形の点） |
| `radar_window` | レーダーを計算する時期（月-日、各年） |
| `pixel_maps` | 圃場内マップ（10m画素ごとの NDVI）を作る。地図の「塗り方」で圃場内（絶対・相対）を選ぶと、選んだ田んぼの中を色分けする |
| `pixel_window` | 圃場内マップを作る期間（月-日）。今年と昨年の分を作る |
| `trend_maps` | 生育傾向マップを作る。年ごとに、4月の NDVI（麦）・5〜6月のレーダー（水張り）・7月の NDVI（稲の育ち）で作物を見分ける手がかりと、6月ごろ・8月ごろの「晴れた日のその田の中央値との差」を保存し、ビューアで稲の年だけを重ねる |
| `trend_from` | 何年からさかのぼるか（いまは2022年から。Sentinel-2 が日本で安定して使えるのは2019年から） |
| `trend_windows` | 「6月ごろ」「8月ごろ」の期間（月-日） |
| `trend_center` | この場所（経度, 緯度）に近いセルから先に作る |
| `cell_deg_lon/lat` | 配信ファイルの分割単位 |

## ローカルでの動作確認（GEE 不要）
```bash
pip install -r requirements.txt
python pipeline/build_cells.py
python pipeline/update_ndvi.py --backend fake --fake-csv sample/genshu_center_ndvi.csv
python pipeline/trend.py --backend fake      # 生育傾向マップ（作り物のデータ）
cd site && python -m http.server 8000   # → http://localhost:8000/
```
`sample/` には原種センター17区画の GeoJSON と NDVI（2025/1〜2026/9）が入っています。

## ライセンス・出典
- 衛星: Contains modified Copernicus Sentinel data（無償・出典表記のみ）
- 区画: 農林水産省「筆ポリゴン」（利用規約に従い出典表記）
- 地図: 国土地理院タイル
- 計算: Google Earth Engine。**非営利（研究・教育）用途は無償、行政の実務運用は有償ライセンス** の対象になり得ます。
  試験場の研究として試作→本運用時に Earth Engine 商用ライセンスか、計算部分を Copernicus openEO に置き換える
  （`pipeline/update_ndvi.py` の `GEEBackend` を差し替えるだけ）のどちらかを判断してください。

## 限界
- 10m 画素なので、細長い区画や小さな区画は隣の影響を受けます（有効画素率で絞り込み可）
- 雲・かすみは完全には除けません。自動除外は「多くの区画が一斉に下がって次で戻る日」だけです
- NDVI は 0.8〜0.9 で頭打ちになるため、繁茂した圃場間の差は実際より小さく見えます
- 生育傾向マップの作物の見分け（年ごと・田ごと。地図の画面で判定するので、基準を変えてもデータは作り直さない）:
  - 麦: 4月の NDVI が 0.6 以上（先に判定）
  - 稲: 7/10〜7/31 の NDVI が 0.5 以上で、5/1〜6/10 のレーダー VH が −21dB 以下（水張り）。
    または、レーダーが −18dB 以下で、6月ごろ（6/1〜7/5）の田の中央値 NDVI が 0.6 未満（小さい田・遅い田植えは水面が見えにくく −18〜−21dB に出るため。
    6月にすでに一面の緑になっている草地などは除く）
  - それ以外: その他（大豆・野菜・休耕など）。直播の稲などは「その他」になることがあり、その年は重ねない
  - 2022〜2026年の県全体（約16万枚×5年）で、稲の年は約66%（福井の水田に占める稲の割合に近い）。
  区画の縁（約8m）は畦・道路が混ざるので計算に使いません
